// Package version reports the psbox build version.
package version

// Current is the build version. Release builds override it with
// -X petris.dev/psbox/internal/version.Current=<version>.
var Current = "dev"
